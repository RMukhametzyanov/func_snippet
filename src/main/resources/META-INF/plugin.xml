<idea-plugin>
    <id>com.testviewer.folder-scanner</id>
    <name>func snippet</name>
    <version>2.0.0</version>
    <vendor email="support@example.com" url="https://www.example.com">Folder Scanner</vendor>

    <description><![CDATA[
    A PyCharm tool window that scans a selected folder and shows a navigable tree of files.
    It also displays Python file structure (classes/functions) and can insert function calls into the editor.
    ]]></description>

    <change-notes><![CDATA[
    Version 2.0.0 - Major rewrite of the plugin UI and folder scanning workflow.
    ]]></change-notes>

    <!-- please see https://plugins.jetbrains.com/docs/intellij/build-number-ranges.html for description -->
    <idea-version since-build="241" until-build="300.*"/>

    <!-- please see https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html
         on how to target different products -->
    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- ToolWindow для боковой панели -->
        <toolWindow id="func snippet" 
                    secondary="true" 
                    anchor="right" 
                    factoryClass="com.testviewer.FolderTreeToolWindowFactory"/>
        
        <!-- Настройки плагина -->
        <applicationConfigurable 
            parentId="tools" 
            instance="com.testviewer.FolderScannerConfigurable"
            id="com.testviewer.FolderScannerConfigurable"
            displayName="func snippet"/>
    </extensions>

</idea-plugin>

